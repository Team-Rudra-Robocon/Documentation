{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "mkdocs serve (venv auto-setup)",
      "type": "shell",
      "command": "if [ ! -d .venv ]; then python3 -m venv .venv; fi && .venv/bin/python -m pip install -r requirements.txt && .venv/bin/mkdocs serve --livereload",
      "options": {
        "cwd": "${workspaceFolder}",
        "shell": {
          "executable": "/bin/bash",
          "args": ["-c"]
        }
      },
      "problemMatcher": []
    }
  ]
}
